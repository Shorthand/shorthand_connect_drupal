# Schema for the configuration files of the shorthand module.

shorthand.settings:
  type: config_object
  label: 'Shorthand settings'
  mapping:
    shorthand_token:
      type: string
      label: 'Shorthand API token'
    keep_previous_versions:
      type: boolean
      label: 'Keep previously downloaded versions'
