services:
  shorthand.api.v2:
    class: Drupal\shorthand\ShorthandApiV2
    arguments: ['@http_client', '@file_system', '@messenger', '@logger.channel.shorthand', '@config.factory']
  logger.channel.shorthand:
    parent: logger.channel_base
    arguments: ['shorthand']
